---
title: Run the app locally
description: Dev servers, builds and the check gate
---

<Steps>
  <Step>

    ### Install tools and dependencies
    ```bash
    mise trust && mise install && mise run setup
    ```

  </Step>
  <Step>

    ### Start the dev servers
    ```bash
    REZEPTE_ADMIN_PASSWORD=change-me mise run dev
    ```
    Go API on `http://localhost:8060` (restarts on `.go` changes), SvelteKit on `http://localhost:9060` (proxies `/api` and `/images` to Go).

    The password is only used on the very first start to create the `admin` user. Log in at `http://localhost:9060/login`.

  </Step>
  <Step>

    ### Run the docs
    ```bash
    mise run docs
    ```
    Fumadocs on `http://localhost:4000`.

  </Step>
  <Step>

    ### Build the production binary
    ```bash
    mise run build
    ./service/bin/rezepte
    ```
    The frontend is built into `service/internal/web/dist` and embedded.

  </Step>
  <Step>

    ### Run the gate
    ```bash
    mise run check
    ```

  </Step>
</Steps>

<Callout type="info">

  Use `mise tasks --all` to list every task, including package-local ones like `//service:test`.

</Callout>

## Known issues

<Callout type="warn">

  - The docs dev server (`mise run docs`) may fail with `spawn EBADF` when started from inside an AI agent's sandboxed shell. Run it from a normal terminal instead; the static build (`mise run //docs:check`) is the gate and works everywhere.
  - Docker Desktop's credential helper (`docker-credential-desktop`) can hang on image pulls on macOS. Use a throwaway `DOCKER_CONFIG` directory without `credsStore` for local builds.
  - If `bunx playwright install chromium` times out downloading, run it once from a normal terminal. `mise run setup` is idempotent and will pick up the cached browser on the next run.

</Callout>
